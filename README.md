# chart-viewer
local-first document viewer designed to organize and browse large collections of PDF files. It utilizes your browser's local storage to index file metadata, enabling rapid searching and filtering without the need for an external server or cloud upload.
